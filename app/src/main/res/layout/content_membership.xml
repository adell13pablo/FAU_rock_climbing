<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.pablo.fau_rock_climbing.MembershipActivity"
    tools:showIn="@layout/app_bar_membership">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/membership_status_title"
        android:text="@string/mem_status"
        android:textAlignment="center"

        android:layout_above="@+id/membership_status"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:id="@+id/membership_status"
        android:text="Status"
        android:layout_centerInParent="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textSize="30pt"
        android:textColor="@color/grey_light"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:id="@+id/membership_validity"
        android:text="Valid until"
        android:layout_centerInParent="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        android:textSize="15pt"
        android:textColor="@color/grey_light"
        android:layout_below="@+id/membership_status"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/renew_payment"
        android:text="@string/renew_payment"
        android:visibility="gone"
        android:background="@color/colorAccent"
        android:layout_below="@+id/membership_validity"
        android:layout_marginTop="10dp"
        android:textColor="@color/white"/>


</RelativeLayout>
